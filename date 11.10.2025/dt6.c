#include<stdio.h>
void main()
{
	int j,i,temp,*start;
	int size=sizeof(a)/sizeof(a[0]);
	start=a;
	//loop for sort array
	for (i=0;i<size;i++)
    {
    	for(j=i+1;j<size;j++)
    	{
    		if(*(start+i)>*(start+j))
    		{temp=*(start+i);
    		*(start+i)
			}
		}
	}

}
